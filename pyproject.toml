[project]
name = "python-learning"
version = "0.1.0"
description = "Python learning project"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.0.0",
    "ipykernel>=6.0.0",
    "ipython>=8.0.0",
]

